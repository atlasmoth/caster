import { View } from "react-native";
import { baseStyles } from "../utils/baseStyles";

export default function SuccessPayment({ navigation, route }: any) {
  console.log(route);
  return <View style={[baseStyles.blackBg]}></View>;
}
